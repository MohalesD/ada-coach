# Session Start: Ada Coach Build Prep

Run these steps in order before doing anything else this session.

1. Run `git status` and tell me what branch I am on and whether there are uncommitted changes.
2. Run `git log --oneline -5` and show me the last 5 commits.
3. Read CLAUDE.md and summarize the top 3 active constraints I should respect this session.
4. Run `/context` and tell me the current context fill percentage.
5. Ask me: "What is the one thing you need to ship this session?" Then wait for my answer before doing anything else.

Do not start any code changes until I have answered step 5.